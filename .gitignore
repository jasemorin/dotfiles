# nvim 运行时产物（不属于配置）
config/nvim/spell/
config/nvim/shada/
config/nvim/sessions/

# macOS
.DS_Store

# 任何可能混入的密钥
*.token
*_rsa
*.pem

# Karabiner 自动备份（每次改配置都会生成，不是配置本身）
config/karabiner/automatic_backups/
config/karabiner/*.bak-*
