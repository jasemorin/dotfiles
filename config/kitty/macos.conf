# ── 仅 macOS 加载（kitty.conf 末尾 include ${KITTY_OS}.conf）──
# Linux 上分屏交给 niri，这些键位不需要；Mac 上保持原样

# ── Layouts ───────────────────────────────────
# splits 必须排第一，否则 --location=vsplit/hsplit 不生效
enabled_layouts splits,stack,tall,fat,grid

# Windows (splits)
map cmd+d       launch --cwd=current --location=vsplit
map cmd+shift+d launch --cwd=current --location=hsplit
map cmd+shift+enter toggle_layout stack

# Navigate between splits (vim hjkl)
map cmd+h       neighboring_window left
map cmd+j       neighboring_window down
map cmd+k       neighboring_window up
map cmd+l       neighboring_window right

# Resize splits (vim hjkl)
map cmd+shift+h resize_window narrower 2
map cmd+shift+j resize_window shorter 2
map cmd+shift+k resize_window taller 2
map cmd+shift+l resize_window wider 2

# Scrollback（原 cmd+k / cmd+shift+k / cmd+shift+j 让位给 hjkl）
map cmd+alt+k      clear_terminal scrollback active
map cmd+shift+up   scroll_home
map cmd+shift+down scroll_end

# cmd+h 被导航占用，kitty 默认的"隐藏应用"移到这里
map cmd+alt+h   hide_macos_app

# Rotate split（翻转当前分屏对的方向：左右 ⇄ 上下）
map cmd+shift+r layout_action rotate

# Move window（把分屏推向某方向，改变布局结构而非移动焦点）
# 注意：cmd+ctrl+l 覆盖了 kitty 默认的 clear_terminal to_cursor_scroll
map cmd+ctrl+h move_window left
map cmd+ctrl+j move_window down
map cmd+ctrl+k move_window up
map cmd+ctrl+l move_window right

# 把当前分屏拆出去成为独立标签页
map cmd+shift+t detach_window new-tab

# 关闭：shift 关分屏，alt 关整个系统窗口
# （cmd+shift+w 覆盖了 kitty 默认的 close_os_window，故把它挪到 cmd+alt+w）
map cmd+shift+w close_window
map cmd+alt+w   close_os_window
