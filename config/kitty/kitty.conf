# ──────────────────────────────────────────────
#  Kitty Terminal Configuration
#  Theme: Catppuccin Mocha (dark)
# ──────────────────────────────────────────────

# ── Font ──────────────────────────────────────
font_family      JetBrains Mono
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        16.0

# Adjust line height and cell width for readability
adjust_line_height 110%
adjust_column_width 0

# ── Cursor ────────────────────────────────────
cursor_shape beam
cursor_beam_thickness 1.8
cursor_blink_interval 0.5
cursor_stop_blinking_after 15.0
cursor_trail 3
cursor_trail_decay 0.1 0.4
cursor_trail_start_threshold 2

# ── Scrollback ────────────────────────────────
scrollback_lines 10000
scrollback_pager less --chop-long-lines --RAW-CONTROL-CHARS +INPUT_LINE_NUMBER

# ── Mouse ─────────────────────────────────────
copy_on_select clipboard
mouse_hide_wait 3.0
url_style curly
open_url_with default
detect_urls yes

# ── Bell ──────────────────────────────────────
enable_audio_bell no
visual_bell_duration 0.0
bell_on_tab "🔔 "

# ── Window ────────────────────────────────────
remember_window_size  yes
initial_window_width  120c
initial_window_height 35c
window_padding_width  8
hide_window_decorations titlebar-only
confirm_os_window_close 0
background_opacity 0.70
dynamic_background_opacity yes
background_blur 64

# ── Tab Bar ───────────────────────────────────
tab_bar_edge bottom
tab_bar_style powerline
tab_powerline_style slanted
tab_bar_min_tabs 2
tab_title_template "{index}: {title[title.rfind('/')+1:]}"

# ── Performance ───────────────────────────────
repaint_delay 10
input_delay 3
sync_to_monitor yes

# ── Shell Integration ─────────────────────────
shell_integration enabled

# ── Keybindings ───────────────────────────────

# Tabs
map cmd+t       new_tab_with_cwd
map cmd+w       close_tab
map cmd+1       goto_tab 1
map cmd+2       goto_tab 2
map cmd+3       goto_tab 3
map cmd+4       goto_tab 4
map cmd+5       goto_tab 5
map cmd+shift+] next_tab
map cmd+shift+[ previous_tab

# 复制粘贴：macOS 上 kitty 默认就是 cmd+c/v，Linux 上默认是 ctrl+shift+c/v，这里统一
map cmd+c       copy_to_clipboard
map cmd+v       paste_from_clipboard

# Font size
map cmd+equal   change_font_size all +1.0
map cmd+minus   change_font_size all -1.0
map cmd+0       change_font_size all 0

# Misc
map cmd+shift+, load_config_file

# Scrollback：跳到顶部/底部、按命令提示符跳、搜索、看上一条命令的输出
# （提示符相关依赖 shell_integration）
map cmd+shift+up   scroll_home
map cmd+shift+down scroll_end
map cmd+up         scroll_to_prompt -1
map cmd+down       scroll_to_prompt 1
map cmd+f          search_scrollback
map cmd+g          show_last_command_output

# Hints：屏幕上的链接/路径标出字母，按字母选中
map cmd+e       open_url_with_hints
map cmd+shift+e kitten hints --type url --program @
map cmd+o       kitten hints --type path
map cmd+shift+o kitten hints --type path --program -

# ── Theme (Catppuccin Mocha) ──────────────────
include current-theme.conf

# ── 按系统加载差异配置 ──────────────────────
# macos.conf：kitty 分屏键位（Mac 专用，保持原样）
# linux.conf：分屏交给 niri，kitty 只保留标签页
include ${KITTY_OS}.conf
