# caps lock：按住 = ctrl，单击 = esc（对应 macOS 上的 Karabiner 设置）
# 系统级配置，不走 install.sh：sudo cp system/keyd/default.conf /etc/keyd/
# 在内核输入层生效，GNOME / niri / TTY 都适用
[ids]
*

[main]
capslock = overload(control, esc)
